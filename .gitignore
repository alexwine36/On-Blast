node_modules
dist
dist-ssr
*.local
.DS_Store
.turbo

# Source recordings kept locally, not distributed.
*.m4a

# The raw source clip stays local; the trimmed sting.wav that the app actually
# plays is committed. See ATTRIBUTION.md.
packages/app-core/assets/audio/sting-source.m4a
